node_modules/
dist/
build/
*.log
.DS_Store
*.pem
*.key
id_rsa*
id_ed25519*

# Runtime / secrets (never commit)
config/local.json
config/host-auth.json
config/host-credentials.txt
config/host-gateway.json
.env
.env.*
!.env.example

client/dist/
server/dist/

# User projects stay local; only the example ships in git
projects/*
!projects/example-article/
!projects/example-article/**

# Build artifacts (even for the example)
projects/*/.openleaf/

# Per-project git backups (local to each paper)
projects/**/.git/

# Editor / OS junk
.idea/
.vscode/
*.swp
*~
# Personal citation library root (runtime data; not the server/src/services/library/ code)
/library/
